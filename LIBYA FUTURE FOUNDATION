import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

export default function Home() {
  return (
    <main className="p-4 space-y-16">
      {/* Hero Section */}
      <section className="text-center py-20 bg-gradient-to-r from-green-700 to-green-900 text-white rounded-2xl shadow-xl">
        <h1 className="text-5xl font-bold mb-4">Libya Future Foundation</h1>
        <p className="text-xl mb-8">Shaping Libya's Future Today</p>
        <Button className="text-lg px-6 py-3 rounded-full">Get Involved</Button>
      </section>

      {/* Vision & Mission */}
      <section className="grid md:grid-cols-2 gap-8">
        <Card className="rounded-2xl shadow-md">
          <CardContent className="p-6">
            <h2 className="text-2xl font-bold mb-2">Our Vision</h2>
            <p>A prosperous, sustainable, and innovative Libya leading the region.</p>
          </CardContent>
        </Card>

        <Card className="rounded-2xl shadow-md">
          <CardContent className="p-6">
            <h2 className="text-2xl font-bold mb-2">Our Mission</h2>
            <p>Empowering Libyan youth and driving projects in green energy, AI, and leadership.</p>
          </CardContent>
        </Card>
      </section>

      {/* Projects */}
      <section>
        <h2 className="text-3xl font-bold mb-6 text-center">Our Projects</h2>
        <div className="grid md:grid-cols-3 gap-6">
          {[
            { title: "Green Energy", desc: "Solar, wind, and renewable projects across Libya." },
            { title: "Artificial Intelligence", desc: "AI solutions for Libya's future economy." },
            { title: "Youth Leadership", desc: "Empowering youth through leadership programs." },
          ].map((proj) => (
            <Card key={proj.title} className="rounded-2xl shadow-md">
              <CardContent className="p-6">
                <h3 className="text-xl font-bold mb-2">{proj.title}</h3>
                <p>{proj.desc}</p>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* Partners */}
      <section>
        <h2 className="text-3xl font-bold mb-6 text-center">Our Partners</h2>
        <div className="flex justify-center space-x-8">
          <div className="bg-gray-100 p-4 rounded-xl shadow">Partner 1</div>
          <div className="bg-gray-100 p-4 rounded-xl shadow">Partner 2</div>
          <div className="bg-gray-100 p-4 rounded-xl shadow">Partner 3</div>
        </div>
      </section>

      {/* Contact */}
      <section>
        <h2 className="text-3xl font-bold mb-6 text-center">Contact Us</h2>
        <form className="max-w-md mx-auto space-y-4">
          <input placeholder="Your Name" className="w-full p-3 border rounded-xl" />
          <input placeholder="Your Email" className="w-full p-3 border rounded-xl" />
          <textarea placeholder="Your Message" className="w-full p-3 border rounded-xl"></textarea>
          <Button className="w-full py-3 rounded-full text-lg">Send Message</Button>
        </form>
      </section>

      {/* Footer */}
      <footer className="text-center text-gray-500 py-8">
        © 2025 Libya Future Foundation. All rights reserved.
      </footer>
    </main>
  );
}
